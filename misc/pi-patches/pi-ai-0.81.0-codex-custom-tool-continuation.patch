--- a/dist/api/openai-codex-responses.js
+++ b/dist/api/openai-codex-responses.js
@@ -32,6 +32,44 @@
 // endpoint (the same endpoint the official Codex client compresses against).
 const REQUEST_COMPRESSION_ZSTD_LEVEL = 3;
 const CODEX_TOOL_CALL_PROVIDERS = new Set(["openai", "openai-codex", "opencode"]);
+const APPLY_PATCH_TOOL_DESCRIPTION = "Use the `apply_patch` tool to edit files. This is a FREEFORM tool, so do not wrap the patch in JSON.";
+const APPLY_PATCH_LARK_GRAMMAR = `start: begin_patch hunk+ end_patch
+begin_patch: "*** Begin Patch" LF
+end_patch: "*** End Patch" LF?
+
+hunk: add_hunk | delete_hunk | update_hunk
+add_hunk: "*** Add File: " filename LF add_line+
+delete_hunk: "*** Delete File: " filename LF
+update_hunk: "*** Update File: " filename LF change_move? change?
+
+filename: /(.+)/
+add_line: "+" /(.*)/ LF -> line
+
+change_move: "*** Move to: " filename LF
+change: (change_context | change_line)+ eof_line?
+change_context: ("@@" | "@@ " /(.+)/) LF
+change_line: ("+" | "-" | " ") /(.*)/ LF
+eof_line: "*** End of File" LF
+
+%import common.LF
+`;
+const CODEX_CUSTOM_TOOLS = new Map([
+    [
+        "apply_patch",
+        {
+            description: APPLY_PATCH_TOOL_DESCRIPTION,
+            format: {
+                type: "grammar",
+                syntax: "lark",
+                definition: APPLY_PATCH_LARK_GRAMMAR,
+            },
+        },
+    ],
+]);
+const CODEX_CUSTOM_TOOL_NAMES = new Set(CODEX_CUSTOM_TOOLS.keys());
+function getCodexCustomTools(model) {
+    return model.provider === "openai-codex" ? CODEX_CUSTOM_TOOLS : undefined;
+}
 const WEBSOCKET_MESSAGE_TOO_BIG_CLOSE_CODE = 1009;
 const WEBSOCKET_CONNECTION_LIMIT_REACHED_CODE = "websocket_connection_limit_reached";
 const CODEX_RESPONSE_STATUSES = new Set([
@@ -340,9 +378,13 @@
 // ============================================================================
 function buildRequestBody(model, context, options) {
     const toolPlacement = splitDeferredTools(context, model.compat?.supportsToolSearch ?? false);
+    const customTools = getCodexCustomTools(model);
+    const customToolNames = customTools === undefined ? undefined : CODEX_CUSTOM_TOOL_NAMES;
     const messages = convertResponsesMessages(model, context, CODEX_TOOL_CALL_PROVIDERS, {
         includeSystemPrompt: false,
         deferredTools: toolPlacement.deferred,
+        customToolNames,
+        customTools,
     });
     const body = {
         model: model.id,
@@ -354,7 +396,7 @@
         include: ["reasoning.encrypted_content"],
         prompt_cache_key: clampOpenAIPromptCacheKey(options?.sessionId),
         tool_choice: options?.toolChoice ?? "auto",
-        parallel_tool_calls: true,
+        parallel_tool_calls: false,
     };
     if (options?.temperature !== undefined) {
         body.temperature = options.temperature;
@@ -363,7 +405,10 @@
         body.service_tier = options.serviceTier;
     }
     if (toolPlacement.immediate.length > 0) {
-        body.tools = convertResponsesTools(toolPlacement.immediate, { strict: null });
+        body.tools = convertResponsesTools(toolPlacement.immediate, {
+            strict: null,
+            customTools,
+        });
     }
     if (options?.reasoningEffort !== undefined) {
         const effort = options.reasoningEffort === "none"
@@ -1115,9 +1160,12 @@
             keepConnection = false;
         }
         else if (useCachedContext && entry && output.responseId) {
+            const customTools = getCodexCustomTools(model);
+            const customToolNames = customTools === undefined ? undefined : CODEX_CUSTOM_TOOL_NAMES;
             const responseItems = convertResponsesMessages(model, { messages: [output] }, CODEX_TOOL_CALL_PROVIDERS, {
                 includeSystemPrompt: false,
-            }).filter((item) => item.type !== "function_call_output");
+                customToolNames,
+            }).filter((item) => item.type !== "function_call_output" && item.type !== "custom_tool_call_output");
             entry.continuation = {
                 lastRequestBody: fullBody,
                 lastResponseId: output.responseId,
