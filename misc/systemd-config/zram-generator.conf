# zram-generator(8)
# Keep disk swap for hibernation/resume; zram has higher priority for runtime swap.

[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
