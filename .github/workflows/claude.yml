name: Claude Code

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *' # 3 PM JST daily

jobs:
  claude:
    runs-on: ubuntu-latest
    steps:
      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: '--model haiku'
          prompt: 'Hi'
